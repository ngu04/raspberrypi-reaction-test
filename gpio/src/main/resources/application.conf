akka {
  loggers = [akka.event.slf4j.Slf4jLogger]
  loglevel = debug
  actor {
    debug {
      receive = on
      lifecycle = on
    }
  }
}

reaction {
  led.pulse.length = 1000
  correction.factor = 100
  threshold = 100
}